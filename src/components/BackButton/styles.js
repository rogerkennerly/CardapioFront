import styled from "styled-components";

export const Container = styled.div`
  font-family: Poppins;
  font-size: 24px;
  font-weight: 700;

  > a {
    display: inline-flex;
    align-items: center;
  }

  > a svg {
    font-size: 44px;
  }
`;
